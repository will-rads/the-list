# W2 task — venue web v3 (glass fork + live wire) (Codex brief)

Work in this repo. Do NOT commit or push. Ponytail rules: shortest working diff, no new
abstractions, no deps beyond the supabase-js CDN.

Read first: docs/superpowers/plans/2026-07-10-backend-v1-plan.md, web/v3/index.html (the finished
v3 glass member file — your reference for tokens, glass recipe, icons, supabase patterns),
web/v2/venue.html (the venue flows you are reskinning).

## Deliverable
`web/v3/venue.html` = fork of `web/v2/venue.html`, reskinned to the v3 glass system and wired to
live Supabase.

## Reskin (copy the system from web/v3/index.html — do not invent)
- Photo ground: `--bg-photo` var + `.iphone-screen::before` scrim, `../assets/bg-3.jpeg` BOTH themes.
- Token block: copy v3 tokens (translucent `--bg-elev/2`, `--ice`, neutral inks/lines,
  `backdrop-filter: blur(26px) saturate(1.9)` glass, light alphas .34/.38).
- NO gray text: `--ink-2`/`--ink-mute` = `--ink`; inactive dock tabs use `--nav-idle`; decorative
  icons + hairlines stay dim. Plus Jakarta Sans ONLY. Sentence case everywhere.
- Icons through the HICONS map pattern (copy the helper from index.html).
- Keep every venue flow/screen from v2 as-is structurally — this is a reskin + wire, not a redesign.

## Supabase wiring
- COPY the exact CDN script tag AND the exact anon key string from web/v3/index.html (lines ~14 and
  ~391-394). Do NOT retype them — a previous run corrupted the JWT by reconstructing it. Copy
  byte-for-byte.
- Auth: email OTP (signInWithOtp → verifyOtp type 'email'), venue accounts are founder-created so
  no signup — login only. After login read own profiles row; if role is not 'venue', show a plain
  "This login has no venue" state.
- Venue's world (RLS already scopes everything to the owner):
  - My venue: from('venues').select('*') (owner sees own).
  - My events: from('events').select('*') — RLS returns published + own drafts; filter client-side.
  - Post event → rpc post_event(p_title, p_kind, p_description, p_image, p_starts, p_ends, p_seats,
    p_price, p_story_hours) — map the v2 create-event form fields onto these.
  - Applicant deck per event: from('applications').select('*, profiles(full_name, ig_handle,
    avatar_url, creator_data, reputation)').eq('event_id', id) — swipe right → rpc
    pick_applicant(p_app), swipe left → rpc skip_applicant(p_app) (no-op by design; deck moves on).
  - Check-in list: applications with status 'confirmed' → rpc check_in(p_app) per attendee.
  - Close event → rpc close_event(p_event) (fires not-selected batch + creates the booking row).
  - Report: applications by status + stories (verdict/score) + bookings row for the event.
  - Notifications: own feed from('notifications') + realtime INSERT channel filtered
    user_id=eq.<uid>; on arrival ALSO re-fetch the venue data so statuses stay truthful (same
    pattern as index.html's hydrate-on-notification).
- Keep the v2 demo/mock data as the logged-out fallback so the file renders before auth.

## Checker
Extend web/v3/check-v3.mjs with a venue.html entry mirroring the index checks (bg-photo +
backdrop-filter required, purple pair banned, Cormorant/Space Grotesk/Inter/Instrument Serif
banned, file must parse). `node web/v3/check-v3.mjs` must pass for BOTH files.

## Constraints
Do not modify web/v3/index.html (except zero changes), web/v2/*, web/index.html, ios/.

## Report
Screens wired + line ranges, form→rpc field mapping for post_event, checker output, skips.
